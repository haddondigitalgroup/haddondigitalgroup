# Public assets

- **favicon.svg** – Site icon (used in layout metadata). Replace or add `favicon.ico` if you prefer.
- **og-image.png** – Add a 1200×630px image for Open Graph (LinkedIn, Facebook). Used in layout metadata; omit if not needed.
- **videos/** – See `videos/README.md`. Add trades.mp4, cafe.mp4, small-business.mp4 (or set env URLs) for the "Who we build for" video section.
